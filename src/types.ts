export interface VideoItem {
  id: string;
  title: string;
  filename?: string;
  url: string;
  thumbnail?: string;
  uploader?: string;
  created_at?: string;
  duration?: string;
  status?: string;
}
